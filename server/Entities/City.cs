namespace server.Entities
{
    // Defining the City Entity with attributes same as the database table
    public class City
    {
        public int ID { get; set; }
        public string Name { get; set; }
    }
}