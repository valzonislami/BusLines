namespace server.Entities
{
    // Defining the Operator Entity with attributes same as the database table
    public class Operator
    {
        public int Id { get; set; }
        public string Name { get; set; }
    }
}